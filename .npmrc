# Dependency scripts are blocked in CI via --ignore-scripts flag in workflows.
# The preinstall guard in package.json blocks "npm install" locally.
# Do NOT add ignore-scripts=true here — it would disable the preinstall guard.
